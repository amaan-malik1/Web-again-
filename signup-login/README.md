## Authentication

## Features

#### 1. Signup

#### 2. Login

#### 3. cookie session

#### 4. jwt token generation

#### 5. User model schema

Install dependencies
`npm install`

Start server:
`npm run dev`

Env vars:

`MONGO_URL = mongodb://localhost:27017/Authentication`

`JWT_SECRET = something_jwt `
